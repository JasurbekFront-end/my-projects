/**
 * Challenge 9: Static Methods & Properties
 *
 * Vazifa: Static metodlar bilan MathUtils classini yarating
 *
 * Bu challenge da quyidagi narsalarni o'rganasiz:
 * 1. Static xususiyatlarni ishlatish
 * 2. Static metodlarni yaratish
 * 3. Class instantiation siz ishlatish
 * 4. Utility class yaratish
 *
 * Talablar:
 * 1. MathUtils classini quyidagi static xususiyatlar bilan yarating:
 *    - PI: number (π soni)
 *    - E: number (e soni)
 *
 * 2. Quyidagi static metodlarni qo'shing:
 *    - sum(numbers: number[]): number - sonlar yig'indisini hisoblash
 *    - average(numbers: number[]): number - sonlar o'rtachasini hisoblash
 *    - max(numbers: number[]): number - eng katta sonni topish
 *    - min(numbers: number[]): number - eng kichik sonni topish
 *
 * 3. Har bir metodda quyidagi tekshirishlarni qo'shing:
 *    - Array bo'sh bo'lmasligi kerak
 *    - Barcha elementlar son bo'lishi kerak
 *
 * Masalan:
 * const numbers = [1, 2, 3, 4, 5];
 *
 * console.log(MathUtils.PI); // 3.141592653589793
 * console.log(MathUtils.E); // 2.718281828459045
 *
 * console.log(MathUtils.sum(numbers)); // 15
 * console.log(MathUtils.average(numbers)); // 3
 * console.log(MathUtils.max(numbers)); // 5
 * console.log(MathUtils.min(numbers)); // 1
 *
 * console.log(MathUtils.sum([])); // Xato - array bo'sh bo'lishi mumkin emas
 * console.log(MathUtils.average([1, 2, "3"])); // Xato - barcha elementlar son bo'lishi kerak
 */

// Sizning yechimingizni shu yerda yozing
