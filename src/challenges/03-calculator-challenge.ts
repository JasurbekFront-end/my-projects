/**
 * Challenge 3: Calculator Class
 *
 * Vazifa: Turli metodlar bilan Calculator classini yarating
 *
 * Bu challenge da quyidagi narsalarni o'rganasiz:
 * 1. Class metodlarini yaratish
 * 2. this kalit so'zidan foydalanish
 * 3. Xatoliklarni boshqarish
 * 4. Class xususiyatlarini o'zgartirish
 *
 * Talablar:
 * 1. Calculator classini quyidagi xususiyat bilan yarating:
 *    - result: number (hisoblash natijasi)
 *
 * 2. Quyidagi metodlarni qo'shing:
 *    - add(num: number): void - son qo'shish
 *    - subtract(num: number): void - son ayirish
 *    - multiply(num: number): void - son ko'paytirish
 *    - divide(num: number): void - son bo'lish
 *    - getResult(): number - natijani olish
 *
 * 3. divide metodida nolga bo'lish xatosini tekshiring
 *
 * 4. Har bir metod this.result ni o'zgartirishi kerak
 *
 * Masalan:
 * const calc = new Calculator();
 * calc.add(5); // result = 5
 * calc.multiply(2); // result = 10
 * calc.subtract(3); // result = 7
 * calc.divide(2); // result = 3.5
 * console.log(calc.getResult()); // 3.5 chiqarishi kerak
 * calc.divide(0); // Xato chiqarishi kerak, chunki nolga bo'lish mumkin emas
 */

// Sizning yechimingizni shu yerda yozing
